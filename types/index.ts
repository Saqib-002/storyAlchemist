export interface SidebarLink {
  imgURL: string;
  route: string;
  label: string;
}
export interface IAudios {
  src: string;
  value: string;
  label: string;
}
export interface ParamsProps {
  params: { id: string };
}
